============
Installation
============

At the command line::

    $ pip install alifelepackage

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv alifelepackage
    $ pip install alifelepackage
